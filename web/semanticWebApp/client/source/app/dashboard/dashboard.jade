.dashboard-view.full-width
    .card.bgc-blue-100(class="p+")
        p Welcome! So you are looking for cool suggestions on papers for further research? Look no further. Click one of the boxes below to get started or select an option from the menu.
    .boxes
        a.box.tc-white(ng-repeat="product in ::vm.products",
            ui-sref="{{::product.link}}",
            class="{{::vm.colors[$index % vm.colors.length]}}"
            )
            i.icon.icon--xl.icon--flat.mdi(class='{{::product.icon}}')
            span.count {{::product.count}}
            p.name {{::product.name}}
